spring:
  jpa:
    properties:
      hibernate:
        ddl-auto: create
        show_sql: true
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

  profiles:
    active: test